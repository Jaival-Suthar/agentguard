{
  "name": "agentguard",
  "version": "0.1.0",
  "private": true,
  "description": "Runtime assurance for autonomous agents.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.14.0"
  }
}
